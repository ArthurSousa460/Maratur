-- CreateTable
CREATE TABLE `Region` (
    `cod_region` INTEGER NOT NULL AUTO_INCREMENT,
    `region_name` VARCHAR(191) NOT NULL,

    PRIMARY KEY (`cod_region`)
) DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
